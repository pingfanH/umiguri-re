# 构建产物
dist/
node_modules/

# 系统文件
.DS_Store
Thumbs.db

# Rust / Tauri
src-tauri/target/
src-tauri/gen/
src-tauri/.cargo/
Cargo.lock

# 加密后的游戏包(由 build:game + encrypt 生成)
dist/main.js.enc

# 临时文件
*.log
*.tmp
*.bak

# 父仓库 .gitignore 的 `main.js` / `build/` / `dist/` 是「任意层级」匹配,
# 会误伤本目录的宿主入口与构建脚本, 这里显式放行。
!build/
!src/host/main.js

